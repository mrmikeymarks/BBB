{
  "name": "site-extract",
  "version": "1.0.0",
  "private": true,
  "description": "Crawl a website with a real browser into an offline-runnable mirror plus per-page text extraction (JSON + Markdown).",
  "main": "src/crawl.js",
  "bin": {
    "site-extract": "src/crawl.js"
  },
  "scripts": {
    "extract": "node src/crawl.js",
    "test": "node test/run-test.js",
    "setup": "npm install && npx playwright install chromium"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "^1.56.1"
  }
}
